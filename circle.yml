machine:
  services:
    - docker
 
dependencies:
  override:
    - docker info
    - docker build -t ikai/go-thumber .
 
test:
  override:
    - docker run -p 81:8081 ikai/go-thumber echo "Hello world!"
